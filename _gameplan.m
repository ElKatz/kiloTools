% game plan

% update all files on the PC...
% AH! the PC is connected to interenet. So this can aactually be done via
% github. So update things wisely.


%% after data collection:

% run 'batch_convertAndSort'
%   2do: incorporate the extraction of strobes from the pl file such that
%   it saves it out with the rest of the ks stuff.

% make a 'figured' directory in the ksDir

% run sp = getSp(ksDir);
% save sp;
% plot some mkfig(sp) stuff and save in ksDir figures

% load sp;
% run su = sp2su(sp);
% save su;
% plot some mkgif(su) stuff and save in ksDir figures

% add color coding per putative area classification

%% now starts the behavioral analysis:

% use James' analysis code to make my own
% work on attention task

% later, mtrfmap, gSac..
